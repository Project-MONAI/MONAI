name: Task05_Prostate
task: segmentation

modality: MRI
datalist: ./azureml_configs/prostate_datalist.json # list of files
dataroot: /data/Task05_Prostate # data location

multigpu: True

class_names: [val_acc_pz, val_acc_tz]

azureml_config:
  compute_cluster_name: "dedicated-nc24s-v2"
  input_datasets:
  - "medical_decathlon_prostate_task_05"
  default_datastore: "himldatasets"
  # compute_cluster_name: "lite-test-ds2"
  # input_datasets:
  # - "test_dataset"
  # default_datastore: "test_datastore"
